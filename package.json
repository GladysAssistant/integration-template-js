{
  "name": "gladys-integration-template-js",
  "version": "1.0.0",
  "private": true,
  "description": "Official starter template to build an external integration for Gladys Assistant with the JavaScript SDK.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check src/logger.js && node --check src/config.js && node --check src/weather.js && node --check src/devices.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@gladysassistant/integration-sdk": "^0.1.0"
  },
  "license": "Apache-2.0"
}
